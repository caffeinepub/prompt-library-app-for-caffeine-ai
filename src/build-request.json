{
  "kind": "build_request",
  "title": "Clean Microsoft Word paste across all text inputs while preserving basic formatting",
  "projectName": "Prompt Library",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Implement a paste-cleaning utility that removes Microsoft Word/HTML/CSS junk and yields clean plain text while preserving basic structure (line breaks, bullets, numbering, and tabs/indentation) so that pasting Word content does not insert CSS/HTML comments into the saved text.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "However when I paste something from Microsoft Word, it is pasting weird.",
          "1) Yes\n2) I do want the basic formatting\n3) Everywhere"
        ]
      },
      "acceptanceCriteria": [
        "When pasting content copied from Microsoft Word, the inserted content contains no HTML tags, CSS, <style> blocks, or HTML comments (e.g., no \"<!-- /* Font Definitions */ ... -->\").",
        "Line breaks from the source are preserved (paragraph separation remains visually separated after paste).",
        "Bulleted lists copied from Word paste as readable bullet characters (e.g., •) on their own lines.",
        "Numbered lists copied from Word paste as readable numbering (e.g., \"1.\", \"2.\") on their own lines (as represented in the clipboard plain text).",
        "The cleaning utility normalizes common Word whitespace artifacts (e.g., CRLF vs LF, non-breaking spaces) without collapsing all newlines into a single line."
      ]
    },
    {
      "id": "REQ-2",
      "text": "Apply the Word paste-cleaning behavior to every user-editable text entry surface in the app, including the rich text prompt content editor and all standard text inputs used for titles, search, category creation/rename, and any other editable fields present in the UI.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "3) Everywhere"
        ]
      },
      "acceptanceCriteria": [
        "Pasting Word content into the “Add New Prompt” title field inserts clean plain text (no HTML/CSS junk).",
        "Pasting Word content into the prompt content editor inserts clean content with preserved line breaks and list readability, and does not insert CSS/HTML comment blocks.",
        "Pasting Word content into the search box inserts clean plain text.",
        "Pasting Word content into category text inputs (e.g., new category name, rename category) inserts clean plain text.",
        "All paste cleaning is performed client-side at paste time (before saving), so the stored prompt/category strings do not include Word HTML/CSS junk."
      ]
    },
    {
      "id": "REQ-3",
      "text": "Harden HTML sanitization so that if any HTML is still introduced (e.g., via non-Word sources or browser quirks), Word-specific and unsafe nodes (such as style/script/head/meta/link and HTML comments) never render as visible text in prompt display components.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "Microsoft Word is pasting a huge block of hidden HTML/CSS into your prompt field."
        ]
      },
      "acceptanceCriteria": [
        "If a prompt’s stored content contains a <style>...</style> block or an HTML comment, those contents are not shown as visible text when rendering prompt previews/details.",
        "Prompt rendering continues to support the editor’s allowed basic formatting (bold/italic and line breaks) without showing raw HTML/CSS text."
      ]
    }
  ],
  "constraints": [
    "Do not edit files under frontend/src/components/ui (read-only UI component sources must be composed, not edited).",
    "Do not edit any paths listed in SYSTEM_CONTEXT.frontend.immutablePaths (compose alternatives instead).",
    "Do not add any third-party authentication or external service dependencies; keep paste cleanup fully client-side."
  ],
  "nonGoals": [
    "Do not preserve rich Word styling (fonts, sizes, margins, complex tables) beyond basic plain-text structure (line breaks, bullets, numbering).",
    "Do not change backend schemas or Motoko canister logic for this request."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}